# Classifier approach
# It crops the bounding boxes in the images, and feeds them in a ResNet model. 
# Drawback over FasterRCNN: it doesn't take into account the region context around the boxes. 
raise NotImplementedError